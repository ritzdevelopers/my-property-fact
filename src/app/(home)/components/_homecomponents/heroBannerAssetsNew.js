/** Shared hero banner paths + dimensions — keep preload + render in sync. */
export const BANNER_ALT =
  "Couple on a luxury penthouse balcony overlooking the city at sunset — My Property Fact";

/** Native artwork size — do not crop or resample the source. */
export const BANNER_WIDTH = 2167;
export const BANNER_HEIGHT = 725;
export const BANNER_SRC = "/static/banners/banner_new_3.png";

export const BANNER_DESKTOP = {
  src: BANNER_SRC,
  width: BANNER_WIDTH,
  height: BANNER_HEIGHT,
};

export const BANNER_TABLET = {
  src: BANNER_SRC,
  width: BANNER_WIDTH,
  height: BANNER_HEIGHT,
};

export const BANNER_MOBILE = {
  src: BANNER_SRC,
  width: BANNER_WIDTH,
  height: BANNER_HEIGHT,
};

export const HERO_IMAGE_QUALITY = 100;
export const HERO_IMAGE_SIZES = "2167px";
