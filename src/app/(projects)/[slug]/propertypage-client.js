"use client";

// Active variant: V3 (99acres-style layout with MPF branding).
// To revert to the previous layout, change the import back to "./propertypage" (V1)
// or "./propertypageV2" (V2). V1/V2 files are kept intact for easy rollback.
export { default } from "./propertypageV3";
